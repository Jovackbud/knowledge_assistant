services:
  # Main web service for the application
  - name: knowledge-assistant-app
    type: web
    env: docker
    plan: free
    autoDeploy: true
    branch: main
    healthCheck:
      path: /
      initialDelaySeconds: 90
